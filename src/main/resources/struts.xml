<!DOCTYPE struts PUBLIC "-//Apache Software Foundation//DTD Struts Configuration 2.0//EN"
    "http://struts.apache.org/dtds/struts-2.0.dtd">

<struts>
    <!-- Include Struts defaults -->
    <include file="struts-default.xml"/>
    
    <!-- Configuration for the default package. -->
    <package name="default" extends="struts-default">
        <interceptors>
            <interceptor-stack name="defaultStack">
                <interceptor-ref name="exception" />
                <interceptor-ref name="alias" />
                <interceptor-ref name="servlet-config" />
                <interceptor-ref name="prepare" />
                <interceptor-ref name="i18n" />
                <interceptor-ref name="chain" />
                <interceptor-ref name="model-driven" />
                <interceptor-ref name="fileUpload" />
                <interceptor-ref name="static-params" />
                <interceptor-ref name="params" />
                <interceptor-ref name="conversionError" />
                <interceptor-ref name="validation">
                    <param name="excludeMethods">cancel,execute,delete,edit,list</param>
                </interceptor-ref>
                <interceptor-ref name="workflow">
                    <param name="excludeMethods">input,back,cancel</param>
                </interceptor-ref>
            </interceptor-stack>
        </interceptors>

        <global-results>
            <result name="dataAccessFailure">dataAccessFailure.jsp</result>
        </global-results>

        <global-exception-mappings>
            <exception-mapping exception="org.springframework.dao.DataAccessException"
                result="dataAccessFailure" />
        </global-exception-mappings>
    </package>

    <!-- Configuration for the default package. -->
    <package name="basic" extends="default">
    	<action name="login"><result name="success">/WEB-INF/jsp/user/login.jsp</result></action>
    	<action name="getPassword-input" method="input" >
    		<result name="success">/WEB-INF/jsp/user/getPassword.jsp</result>
    	</action>
    	<action name="getPassword" class="">
    		<result name="success">/WEB-INF/jsp/user/getPassword.jsp</result>
    	</action>
    </package>

    <package name="admin" extends="default" namespace="/admin" >
		<action  name="admin"><result name="success">/WEB-INF/jsp/admin/adminmain.jsp</result></action>
    </package>    
    
    <package name="rss" extends="rss-default" namespace="/rss">
	    <action name="jobs" class="org.jrecruiter.web.actions.JobsRssAction">
	        <result type="rss" name="success">
	            <param name="feedName">rssFeed</param>
	            <!-- this is not required: defaults to rss 0.9 -->
	            <param name="feedType">rss_1.0</param>
	        </result>
	    </action>
	</package>
    
    <constant name="struts.codebehind.pathPrefix" value="/WEB-INF/jsp/"/>
    
</struts>
